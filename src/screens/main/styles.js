import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        height: '100%',
        width: '100%',
        backgroundColor: '#0a9',
        alignItems: 'center'
    },
    title: {
        color: '#fff',
        fontSize: 45,
        fontWeight: 'bold',
    },
    list: {
        marginTop: 20
    }
})