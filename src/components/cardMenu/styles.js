import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        width: 125,
        height: 125,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
        margin: 30,
        borderRadius: 10,
        borderWidth: 2,
        borderColor: '#000',
        padding: 10,
    },
    title: {
        color: '#000',
        fontSize: 22,
        fontWeight: 'bold',
    }
})